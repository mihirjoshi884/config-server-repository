# ============================
# RESOURCE SERVER (JWT VALIDATION)
# ============================
# Your authorization server’s issuer (must match AuthorizationServerSettings)
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://auth-server
