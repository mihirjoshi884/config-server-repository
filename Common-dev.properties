# ============================
# RESOURCE SERVER (JWT VALIDATION)
# ============================
# Your authorization server’s issuer (must match AuthorizationServerSettings)
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8081

# OPTIONAL: JWK set URI (Spring usually resolves it automatically)
# spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8081/oauth2/jwks
